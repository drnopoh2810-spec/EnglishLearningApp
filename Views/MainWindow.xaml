<Window x:Class="EnglishLearningApp.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="English Learning App"
        Height="800"
        Width="1200"
        WindowStartupLocation="CenterScreen"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        Background="{DynamicResource MaterialDesignPaper}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="14"
        FontFamily="{materialDesign:MaterialDesignFont}"
        WindowStyle="SingleBorderWindow">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesignTheme.Dark.xaml" />
                <ResourceDictionary Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesignTheme.Defaults.xaml" />
                <ResourceDictionary Source="pack://application:,,,/MaterialDesignColors;component/Themes/Recommended/Primary/MaterialDesignColor.DeepPurple.xaml" />
                <ResourceDictionary Source="pack://application:,,,/MaterialDesignColors;component/Themes/Recommended/Accent/MaterialDesignColor.Lime.xaml" />
            </ResourceDictionary.MergedDictionaries>

            <!-- Navigation Button Style -->
            <Style x:Key="NavButtonStyle" TargetType="Button" BasedOn="{StaticResource MaterialDesignFlatButton}">
                <Setter Property="Height" Value="48" />
                <Setter Property="HorizontalContentAlignment" Value="Left" />
                <Setter Property="Padding" Value="16,0" />
                <Setter Property="FontSize" Value="14" />
                <Setter Property="Foreground" Value="{DynamicResource MaterialDesignBody}" />
            </Style>

            <!-- Card Style -->
            <Style x:Key="StatCardStyle" TargetType="materialDesign:Card">
                <Setter Property="Padding" Value="20" />
                <Setter Property="Margin" Value="8" />
                <Setter Property="UniformCornerRadius" Value="8" />
            </Style>

            <!-- Boolean to Visibility Converter -->
            <BooleanToVisibilityConverter x:Key="BoolToVisibilityConv" />
        </ResourceDictionary>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="240" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!-- Navigation Sidebar -->
        <materialDesign:Card Grid.Column="0"
                            Background="{DynamicResource MaterialDesignCardBackground}"
                            UniformCornerRadius="0"
                            Margin="0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <!-- Logo/Header -->
                <Border Grid.Row="0" Background="{DynamicResource PrimaryHueMidBrush}" Height="80">
                    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                        <TextBlock Text="ENGLISH"
                                  FontSize="22"
                                  FontWeight="Bold"
                                  Foreground="{DynamicResource PrimaryHueMidForegroundBrush}" />
                        <TextBlock Text="LEARNING APP"
                                  FontSize="12"
                                  Foreground="{DynamicResource PrimaryHueMidForegroundBrush}"
                                  Opacity="0.8" />
                    </StackPanel>
                </Border>

                <!-- Navigation Buttons -->
                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                    <StackPanel Margin="0,8">
                        <Button Style="{StaticResource NavButtonStyle}"
                               Command="{Binding NavigateToDashboardCommand}">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="ViewDashboard" Width="24" Height="24" Margin="0,0,12,0" />
                                <TextBlock Text="Dashboard" VerticalAlignment="Center" />
                            </StackPanel>
                        </Button>

                        <Button Style="{StaticResource NavButtonStyle}"
                               Command="{Binding NavigateToSentencesCommand}">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="FormatListText" Width="24" Height="24" Margin="0,0,12,0" />
                                <TextBlock Text="Sentences" VerticalAlignment="Center" />
                            </StackPanel>
                        </Button>

                        <Button Style="{StaticResource NavButtonStyle}"
                               Command="{Binding NavigateToGroupsCommand}">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="Folder" Width="24" Height="24" Margin="0,0,12,0" />
                                <TextBlock Text="Groups" VerticalAlignment="Center" />
                            </StackPanel>
                        </Button>

                        <Button Style="{StaticResource NavButtonStyle}"
                               Command="{Binding NavigateToReviewCommand}">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="Cards" Width="24" Height="24" Margin="0,0,12,0" />
                                <TextBlock Text="Review" VerticalAlignment="Center" />
                            </StackPanel>
                        </Button>

                        <Separator Margin="16,8" Background="{DynamicResource MaterialDesignDivider}" />

                        <Button Style="{StaticResource NavButtonStyle}"
                               Command="{Binding NavigateToImportExportCommand}">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="ImportExport" Width="24" Height="24" Margin="0,0,12,0" />
                                <TextBlock Text="Import / Export" VerticalAlignment="Center" />
                            </StackPanel>
                        </Button>

                        <Button Style="{StaticResource NavButtonStyle}"
                               Command="{Binding NavigateToStatisticsCommand}">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="ChartBar" Width="24" Height="24" Margin="0,0,12,0" />
                                <TextBlock Text="Statistics" VerticalAlignment="Center" />
                            </StackPanel>
                        </Button>

                        <Separator Margin="16,8" Background="{DynamicResource MaterialDesignDivider}" />

                        <Button Style="{StaticResource NavButtonStyle}"
                               Command="{Binding NavigateToSettingsCommand}">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="Cog" Width="24" Height="24" Margin="0,0,12,0" />
                                <TextBlock Text="Settings" VerticalAlignment="Center" />
                            </StackPanel>
                        </Button>
                    </StackPanel>
                </ScrollViewer>

                <!-- Theme Toggle -->
                <Border Grid.Row="2" Padding="16" BorderBrush="{DynamicResource MaterialDesignDivider}" BorderThickness="0,1,0,0">
                    <Button Style="{StaticResource MaterialDesignFlatButton}"
                           Command="{Binding ToggleThemeCommand}"
                           HorizontalAlignment="Stretch">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon x:Name="ThemeIcon"
                                                    Kind="WeatherNight" 
                                                    Width="20" 
                                                    Height="20" 
                                                    Margin="0,0,8,0" />
                            <TextBlock x:Name="ThemeText"
                                      Text="Dark Mode" 
                                      VerticalAlignment="Center" />
                        </StackPanel>
                    </Button>
                </Border>
            </Grid>
        </materialDesign:Card>

        <!-- Main Content Area -->
        <Grid Grid.Column="1" Background="{DynamicResource MaterialDesignPaper}">
            <ContentControl Content="{Binding CurrentView}"
                          Margin="24" />
        </Grid>
    </Grid>
</Window>
