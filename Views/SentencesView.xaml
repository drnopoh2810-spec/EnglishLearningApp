<UserControl x:Class="EnglishLearningApp.Views.SentencesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="900">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConv" />
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock Text="Sentences"
                      FontSize="28"
                      FontWeight="Bold" />
            <Button Grid.Column="1"
                   Style="{StaticResource MaterialDesignRaisedButton}"
                   Command="{Binding AddNewCommand}"
                   Background="{DynamicResource PrimaryHueMidBrush}">
                <StackPanel Orientation="Horizontal">
                    <materialDesign:PackIcon Kind="Plus" Margin="0,0,8,0" />
                    <TextBlock Text="Add Sentence" />
                </StackPanel>
            </Button>
        </Grid>

        <!-- Search and Filters -->
        <Grid Grid.Row="1" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="200" />
                <ColumnDefinition Width="150" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <TextBox Grid.Column="0"
                    Style="{StaticResource MaterialDesignOutlinedTextBox}"
                    materialDesign:HintAssist.Hint="Search English or Arabic..."
                    Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                    Margin="0,0,8,0" />

            <ComboBox Grid.Column="1"
                     Style="{StaticResource MaterialDesignOutlinedComboBox}"
                     materialDesign:HintAssist.Hint="Group"
                     ItemsSource="{Binding Groups}"
                     DisplayMemberPath="GroupName"
                     SelectedValuePath="Id"
                     SelectedValue="{Binding SelectedGroupId}"
                     Margin="0,0,8,0" />

            <ComboBox Grid.Column="2"
                     Style="{StaticResource MaterialDesignOutlinedComboBox}"
                     materialDesign:HintAssist.Hint="Difficulty"
                     ItemsSource="{Binding Source={StaticResource DifficultyValues}}"
                     SelectedValue="{Binding SelectedDifficulty}"
                     Margin="0,0,8,0" />

            <Button Grid.Column="3"
                   Style="{StaticResource MaterialDesignFlatButton}"
                   Command="{Binding ClearFiltersCommand}">
                <materialDesign:PackIcon Kind="FilterRemove" />
            </Button>
        </Grid>

        <!-- Add New Sentence Panel -->
        <materialDesign:Card Grid.Row="2"
                            Padding="20"
                            Margin="0,0,0,16"
                            UniformCornerRadius="8"
                            Visibility="{Binding IsAddingNew, Converter={StaticResource BoolToVisibilityConv}}">
            <StackPanel>
                <TextBlock Text="Add New Sentence"
                          FontSize="20"
                          FontWeight="Bold"
                          Margin="0,0,0,16" />

                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <!-- English Sentence -->
                    <TextBox Grid.Row="0" Grid.Column="0"
                            Style="{StaticResource MaterialDesignOutlinedTextBox}"
                            materialDesign:HintAssist.Hint="English Sentence *"
                            Text="{Binding NewEnglishSentence}"
                            Margin="0,0,0,8" />

                    <Button Grid.Row="0" Grid.Column="1"
                           Style="{StaticResource MaterialDesignFlatButton}"
                           Command="{Binding TranslateCommand}"
                           IsEnabled="{Binding IsTranslating, Converter={StaticResource InverseBooleanConverter}}"
                           Margin="8,0,0,8"
                           VerticalAlignment="Top">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="Translate" Margin="0,0,4,0" />
                            <TextBlock Text="Translate" />
                        </StackPanel>
                    </Button>

                    <!-- Arabic Translation -->
                    <TextBox Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2"
                            Style="{StaticResource MaterialDesignOutlinedTextBox}"
                            materialDesign:HintAssist.Hint="Arabic Translation (auto-filled)"
                            Text="{Binding NewArabicTranslation}"
                            FlowDirection="RightToLeft"
                            Margin="0,0,0,8" />

                    <!-- Difficulty and Group -->
                    <Grid Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2" Margin="0,0,0,8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>

                        <ComboBox Grid.Column="0"
                                 Style="{StaticResource MaterialDesignOutlinedComboBox}"
                                 materialDesign:HintAssist.Hint="Difficulty Level"
                                 ItemsSource="{Binding Source={StaticResource DifficultyValues}}"
                                 SelectedValue="{Binding NewDifficulty}"
                                 Margin="0,0,8,0" />

                        <ComboBox Grid.Column="1"
                                 Style="{StaticResource MaterialDesignOutlinedComboBox}"
                                 materialDesign:HintAssist.Hint="Group"
                                 ItemsSource="{Binding Groups}"
                                 DisplayMemberPath="GroupName"
                                 SelectedValuePath="Id"
                                 SelectedValue="{Binding NewGroupId}" />
                    </Grid>

                    <!-- Notes -->
                    <TextBox Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="2"
                            Style="{StaticResource MaterialDesignOutlinedTextBox}"
                            materialDesign:HintAssist.Hint="Notes (optional)"
                            Text="{Binding NewNotes}"
                            Margin="0,0,0,16" />

                    <!-- Buttons -->
                    <StackPanel Grid.Row="4" Grid.Column="0" Grid.ColumnSpan="2" 
                               Orientation="Horizontal">
                        <Button Style="{StaticResource MaterialDesignRaisedButton}"
                               Command="{Binding SaveNewCommand}"
                               Background="{DynamicResource PrimaryHueMidBrush}"
                               Margin="0,0,8,0">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="ContentSave" Margin="0,0,8,0" />
                                <TextBlock Text="Save" />
                            </StackPanel>
                        </Button>
                        <Button Style="{StaticResource MaterialDesignOutlinedButton}"
                               Command="{Binding CancelAddCommand}">
                            <TextBlock Text="Cancel" />
                        </Button>
                    </StackPanel>
                </Grid>
            </StackPanel>
        </materialDesign:Card>

        <!-- Sentences List -->
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding Sentences}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <materialDesign:Card Padding="16" 
                                            Margin="0,0,0,8" 
                                            UniformCornerRadius="8">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                </Grid.RowDefinitions>

                                <!-- English Sentence -->
                                <TextBlock Grid.Row="0"
                                          Text="{Binding EnglishSentence}"
                                          FontSize="16"
                                          FontWeight="SemiBold"
                                          TextWrapping="Wrap"
                                          Margin="0,0,0,8" />

                                <!-- Arabic Translation -->
                                <TextBlock Grid.Row="1"
                                          Text="{Binding ArabicTranslation}"
                                          FontSize="14"
                                          Opacity="0.8"
                                          TextWrapping="Wrap"
                                          FlowDirection="RightToLeft"
                                          Margin="0,0,0,8" />

                                <!-- Meta Info and Actions -->
                                <Grid Grid.Row="2">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>

                                    <StackPanel Grid.Column="0" 
                                               Orientation="Horizontal">
                                        <Border Background="{DynamicResource PrimaryHueMidBrush}" 
                                               CornerRadius="4" 
                                               Padding="8,2" 
                                               Margin="0,0,8,0">
                                            <TextBlock Text="{Binding DifficultyLevel}" 
                                                      FontSize="11"
                                                      Foreground="{DynamicResource PrimaryHueMidForegroundBrush}" />
                                        </Border>
                                        <Border Background="{Binding MasteryScore, Converter={StaticResource MasteryToBrushConverter}}" 
                                               CornerRadius="4" 
                                               Padding="8,2" 
                                               Margin="0,0,8,0">
                                            <TextBlock Text="{Binding MasteryScore, StringFormat='{}{0:F0}%'}" 
                                                      FontSize="11"
                                                      Foreground="White" />
                                        </Border>
                                        <TextBlock Text="{Binding GroupNames}" 
                                                  FontSize="11" 
                                                  Opacity="0.6"
                                                  VerticalAlignment="Center" />
                                    </StackPanel>

                                    <StackPanel Grid.Column="1" 
                                               Orientation="Horizontal">
                                        <Button Style="{StaticResource MaterialDesignFlatButton}"
                                               Command="{Binding DataContext.OpenYouGlishCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                               CommandParameter="{Binding}"
                                               Padding="4"
                                               ToolTip="Open in YouGlish">
                                            <materialDesign:PackIcon Kind="Video" Width="18" Height="18" />
                                        </Button>
                                        <Button Style="{StaticResource MaterialDesignFlatButton}"
                                               Command="{Binding DataContext.PlayPronunciationCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                               CommandParameter="{Binding}"
                                               Padding="4"
                                               ToolTip="Play Pronunciation">
                                            <materialDesign:PackIcon Kind="VolumeHigh" Width="18" Height="18" />
                                        </Button>
                                        <Button Style="{StaticResource MaterialDesignFlatButton}"
                                               Command="{Binding DataContext.DeleteCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                               CommandParameter="{Binding}"
                                               Padding="4"
                                               Foreground="Red"
                                               ToolTip="Delete">
                                            <materialDesign:PackIcon Kind="Delete" Width="18" Height="18" />
                                        </Button>
                                    </StackPanel>
                                </Grid>
                            </Grid>
                        </materialDesign:Card>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>
